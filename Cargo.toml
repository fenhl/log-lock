[package]
name = "log-lock"
version = "0.3.0"
edition = "2024"
rust-version = "1.91" # nixpkgs stable

[features]
always-log = []

[dependencies]
dashmap = "6"
derivative = "2"
parking_lot = "0.12"
tokio = { version = "1", features = ["macros", "rt", "sync", "time"] }
